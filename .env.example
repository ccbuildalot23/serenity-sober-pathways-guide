
# Supabase Configuration
# These are the only environment variables that should be exposed to the client
VITE_SUPABASE_URL=https://your_project_id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# SECURITY WARNING: 
# DO NOT add any sensitive keys that start with VITE_ as they will be exposed to the client
# Server-side secrets like ENCRYPTION_SECRET should ONLY be configured in Supabase Edge Functions secrets
# NEVER expose private keys, service role keys, or encryption secrets to the client

# Example of what NOT to include:
# VITE_ENCRYPTION_SECRET=...  # NEVER DO THIS
# VITE_SUPABASE_SERVICE_ROLE_KEY=...  # NEVER DO THIS
# VITE_PRIVATE_KEY=...  # NEVER DO THIS

# Production Security Checklist:
# 1. Set VITE_SUPABASE_URL to your actual Supabase project URL
# 2. Set VITE_SUPABASE_ANON_KEY to your actual Supabase anon key
# 3. Configure Supabase Auth redirect URLs in your Supabase dashboard
# 4. Enable RLS policies on all tables
# 5. Review and test all security policies before deployment
