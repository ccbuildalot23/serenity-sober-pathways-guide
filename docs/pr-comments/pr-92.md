PR #92 (fix/seed-and-tests)

Changes:
- Added minimal CI workflow: Jest unit + Playwright Chromium (portal checkout via token; Chromium-only install; prewarm)
- Consolidated vercel.json; preview ignored for RC; headers configured
- Added smoke (CSV) and uptime workflows; Supabase verify workflow
- Docs: pilot-kit, runbook, launch checklist, secrets guide, go/no-go

CI Run:
- Link: (add latest Actions URL)

Notes:
- If webServer timeouts occur, prewarm step runs before Playwright
- Only essential smoke specs run on CI per config

Next:
- Verify secrets in GitHub/Vercel UIs; set auto-merge when green

