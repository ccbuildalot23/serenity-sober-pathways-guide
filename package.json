{
  "name": "serenity-mvp",
  "version": "1.0.0",
  "scripts": {
    "start": "node true-mvp-simple.js",
    "build": "mkdir -p dist && cp index.html dist/",
    "vercel-build": "mkdir -p dist && cp index.html dist/"
  },
  "engines": {
    "node": ">=18.x"
  }
}